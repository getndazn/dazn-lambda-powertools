{
  "private": true,
  "name": "@perform/lambda-powertools-http-client",
  "version": "0.1.0",
  "description": "HTTP client that supports forwarding correlation IDs (captured via @perform/lambda-powertools-correlation-ids) and auto-records metrics",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Yan Cui",
  "dependencies": {
    "@perform/lambda-powertools-correlation-ids": "^0.1.0",
    "@perform/lambda-powertools-logger": "^0.1.0",
    "@perform/lambda-powertools-datadog-metrics": "^0.1.0",
    "superagent": "^3.8.3",
    "superagent-promise": "^1.1.0"
  }
}
