{
  "name": "@perform/lambda-powertools-step-functions-client",
  "version": "0.6.4",
  "description": "SFN client wrapper that knows how to forward correlation IDs (captured via @perform/lambda-powertools-correlation-ids)",
  "main": "index.js",
  "scripts": {
    "test": "jest"
  },
  "author": "Yan Cui",
  "dependencies": {
    "@perform/lambda-powertools-correlation-ids": "^0.6.4",
    "@perform/lambda-powertools-logger": "^0.6.4",
    "aws-sdk": "^2.246.1"
  },
  "devDependencies": {
    "jest": "^23.1.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
