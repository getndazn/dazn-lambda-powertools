{
  "name": "@perform/lambda-powertools-lambda-client",
  "version": "0.2.12",
  "description": "Lambda client wrapper that knows how to forward correlation IDs (captured via @perform/lambda-powertools-correlation-ids)",
  "main": "index.js",
  "scripts": {
    "test": "jest"
  },
  "author": "Yan Cui",
  "dependencies": {
    "@perform/lambda-powertools-logger": "^0.2.12",
    "@perform/lambda-powertools-correlation-ids": "^0.2.12",
    "aws-sdk": "^2.246.1"
  },
  "devDependencies": {
    "jest": "^23.1.0"
  }
}
