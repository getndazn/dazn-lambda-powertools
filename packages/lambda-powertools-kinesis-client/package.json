{
  "name": "@perform/lambda-powertools-kinesis-client",
  "version": "0.5.0",
  "description": "Kinesis client wrapper that knows how to forward correlation IDs (captured via @perform/lambda-powertools-correlation-ids)",
  "main": "index.js",
  "scripts": {
    "test": "jest"
  },
  "author": "Yan Cui",
  "dependencies": {
    "@perform/lambda-powertools-correlation-ids": "^0.5.0",
    "@perform/lambda-powertools-logger": "^0.5.0",
    "aws-sdk": "^2.246.1"
  },
  "devDependencies": {
    "jest": "^23.1.0"
  }
}
